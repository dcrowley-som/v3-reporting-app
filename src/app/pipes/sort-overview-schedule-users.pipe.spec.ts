import { SortOverviewScheduleUsersPipe } from './sort-overview-schedule-users.pipe';

describe('SortOverviewScheduleUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOverviewScheduleUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
