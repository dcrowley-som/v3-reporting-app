import { EpisodeMonthlyColumnPipe } from './episode-monthly-column.pipe';

describe('EpisodeMonthlyColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new EpisodeMonthlyColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
