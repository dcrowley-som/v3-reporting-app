import { SortOverviewSchedulesPipe } from './sort-overview-schedules.pipe';

describe('SortOverviewSchedulesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOverviewSchedulesPipe();
    expect(pipe).toBeTruthy();
  });
});
